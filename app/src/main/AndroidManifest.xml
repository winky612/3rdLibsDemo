<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zkhz.a3rdlibsdemo">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".blur.BlurDemoActivity"></activity>
        <activity android:name=".recyclerview.RVActivity"></activity>
        <activity android:name=".recyclerview.boy.BoyRVActivity"></activity>
        <activity android:name=".recyclerview.girl.GirlRVActivity"></activity>
        <activity android:name=".recyclerview.animal.AnimalRVActivity"></activity>
        <activity android:name=".recyclerview.setting.SettingActivtiy"/>
        <activity android:name=".recyclerview.mine.MineActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>